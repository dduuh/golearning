package main

// func Rev(str string) string {
// 	runes := []rune(str)
// 	length := len(runes)

// 	for i := 0; i < length/2; i++ {
// 		j := length - 1 - i
// 		runes[i], runes[j] = runes[j], runes[i]
// 	}

// 	return string(runes)
// }

func main() {

}
